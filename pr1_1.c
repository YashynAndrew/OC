#include <stdio.h>
#include <math.h>

int main(){
	double x;
	printf("Input x \n");
	scanf("%lf",&x);
	printf("f(x) = exp(-|x|)sin(x) = ");
	f(x);
	printf("g(x) = exp(-|x|)cos(x) = ");
	g(x);
	return 0;
	
	
	}
	

